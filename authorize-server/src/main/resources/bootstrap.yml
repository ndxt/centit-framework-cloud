eureka:
  client:
    serviceUrl:
      defaultZone: http://192.168.134:10080/eureka/

server:
  port: 10087

spring:
  application:
    name: AUTHORIZE-SERVICE
  cloud:
    config:
      label: master
      profile: test
      discovery:
        enabled: true
        serviceId: CONFIG-SERVER
