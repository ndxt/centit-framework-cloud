eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:10080/eureka/

server:
  port: 10087

spring:
  application:
    name: AUTHORIZE-SERVICE
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true
        serviceId: CONFIG-SERVER
