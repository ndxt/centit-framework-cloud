eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:10080/eureka/

server:
  port: 10088

spring:
  application:
    name: GATEWAY-SERVICE
  cloud:
    config:
      label: master
      profile: test
      discovery:
        enabled: true
        serviceId: CONFIG-SERVER
